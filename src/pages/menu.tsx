import Navbar from '@/components/Navbar';
import MenuModal from '@/components/Menu/MenuModal';

const Menu = () => {
  return (
    <main>
      <Navbar />
      <MenuModal />
    </main>
  );
};

export default Menu;
